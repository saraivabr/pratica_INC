"use client"

import { AppShell } from "@/components/app-shell"

export default function RecepcionistaLayout({ children }: { children: React.ReactNode }) {
  return <AppShell title="Recepcionista">{children}</AppShell>
}
